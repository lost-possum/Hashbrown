Example Usage

-----------------------------------------------------------------------------

file_path = " "

algorithm = " "  # Replace with your desired algorithm (e.g., "md5", "sha1", etc.)

hash_value = calculate_file_hash(file_path, algorithm)

if hash_value:

print(f"The {algorithm} hash of the file is: {hash_value}")